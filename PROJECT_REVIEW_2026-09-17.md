# 项目复核与修复记录

评估对象：当前工作区，包括任务开始前已经存在的未提交改动。范围按已确认计划执行：恢复编译和测试、补齐操作保护与健康报告链路、优化相关模块、提供可复核的质量结论。没有把注册表中只有规划声明的生态扩展为未经确认的新功能。

项目已恢复构建能力，关键操作链路的缺陷已修复。健康中心的职责划分与回归覆盖明显改善；整个仓库仍存在较多历史工程债，不能把本轮结果等同于所有生态在三平台上的完整验收。

## 已修复的问题

| 问题与触发条件 | 修复后的行为 | 主要验证 |
| --- | --- | --- |
| 健康中心缺少样式导入，组件测试导入不存在的路径 | 渲染层与 Electron 类型检查、生产构建可以通过；组件测试真正执行 | `npm run build`、`npm test` |
| 未选择项目时，列表报告被赋为 `null`，后续 `.slice()` 崩溃 | 列表使用空数组，单项报告使用空值；报告工厂检查载荷与 setter 的类型匹配 | 完整健康中心空项目渲染与刷新测试 |
| A 项目晚返回覆盖 B 项目、旧刷新覆盖新刷新 | 页面状态以项目为边界重建；报告加载器用刷新代次与单块请求标识拒绝旧结果；卸载后不提交结果 | A/B 项目乱序响应、重试乱序、卸载、失败隔离测试 |
| 报告加载中被标为 ready，错误/重试接线未完成 | 加载、成功、失败分别表达；单块与全部失败报告重试只影响相应区块 | 报告加载器与错误组件测试 |
| 主进程保护与适配器重复获取同一项目锁，造成自等待 | 同一异步事务可复用已持有的锁；独立操作仍串行；Windows 路径别名使用同一项目标识 | 嵌套保护与并发写入测试 |
| 已取消的排队操作仍可能打快照或执行纯文件修改 | 排队取消立即返回；真正获得执行权后再次检查取消；异步快照完成后再次检查 | 取消排队写入、快照期间取消测试 |
| 已取消的命令仍先启动进程，终止父进程后过早恢复文件 | 预取消不启动进程；运行中的命令终止进程树，并等待结束后交还控制权；无法确认终止时跳过自动恢复并报告原因 | 真实父子进程、延迟写入、输出字节限制和超时测试 |
| 取消后跳过清单恢复，恢复实现重复 | 普通失败、取消、超时复用恢复模块；已声明文件的创建/删除可恢复；冲突或恢复失败保留原始失败和备份信息 | 适配器取消恢复、首次生成锁文件、恢复冲突测试 |
| 自定义 Error 字段在 Electron IPC / contextBridge 中丢失 | 管理器执行返回普通数据失败封套，穿过两层边界后由渲染层还原错误，保留类别、备份与恢复信息 | 隔离 preload 的真实 Electron IPC 验证 |
| 切换项目/管理器后，旧命令输出覆盖当前页面或清除新任务状态 | 命令反馈按操作范围及 ID 接收；过期操作不更新当前页、不触发旧项目刷新 | 工作台命令反馈测试 |
| 并发注册/取消文件监听会留下过期监听器，部分锁文件未覆盖 | 注册请求可失效；监听器使用明确类型；增加 uv、Poetry、Bun、SwiftPM、Gradle、Native 等根目录清单/锁文件 | 取消待注册监听、创建 uv.lock、原子替换等回归 |
| README 中代码围栏被转义，示例无法正常渲染 | 恢复标准 Markdown 代码围栏与行内代码 | 文本检查 |

恢复的范围是操作声明的项目清单、锁文件和配置文件，不包括已安装环境、下载缓存、远程集群或已发布软件包。内置管理器的保护入口提供串行化与快照，不能据此宣称所有内置命令都实现自动回滚。

## 补齐与重构

- 健康中心入口从本轮开始时的 **7,967 行降为 464 行**。报告目录、状态、派生数据、请求生命周期、操作与展示分别有明确入口。
- 77 个原有操作按 inventory、evidence、automation、change、workspace、release、policy 七个职责组织；面板通过明确的 `Pick` 属性类型依赖所需数据。
- 报告载荷不再通过 `never` 和强制函数类型转换传递，类型检查会拒绝把空值赋给数组状态。
- 命令运行器拆分为输出/进程生命周期、进程树终止和恢复模块；工作台的执行反馈也从大组件中移出。
- 本轮从大健康中心拆出的模块均低于 1,500 行，函数低于 100 行。健康功能目录最大文件为 681 行；此前独立概览模块仍有 5 个超长函数，计入历史债务。
- Vite/Vitest 配置使用明确的 ESM `.mts` 格式及 `import.meta.url` 路径解析；Node.js 最低版本声明同步为 22.12。
- 新增 `npm test`、`verify:ipc`；CI 增加 Windows/Linux 矩阵，去除重复执行的检查。Linux CI 通过 Xvfb 执行 Electron IPC 验证。工作流已更新，本地验证环境为 Windows，尚未取得远端 CI 运行结果。
- 旧框架检查沿真实的本地模块导入读取功能源码；不再要求界面文案和行为全部堆在一个文件里。既有源码断言仍属于结构检查，新增行为测试承担运行时回归。

主要维护入口：

| 职责 | 路径 |
| --- | --- |
| 健康页面组合 | `src/features/health/HealthCenter.tsx` |
| 报告类型绑定及加载生命周期 | `healthReportCatalog.ts`、`reportBlocks.ts`、`useHealthReportLoader.ts` |
| 健康数据与派生值 | `useHealthState.ts`、`useHealthDerived.ts`、`useHealthCenterModel.ts` |
| 报告操作与展示 | `src/features/health/actions/`、`src/features/health/panels/` |
| 写入队列与取消保护 | `electron/services/projectMutation.ts`、`projectGuard.ts`、`operationContext.ts` |
| 进程执行及恢复 | `commandRunner.ts`、`commandProcess.ts`、`processTree.ts`、`commandRecovery.ts` |
| IPC 失败协议与渲染层客户端 | `shared/ipcFailure.ts`、`electron/ipcHandler.ts`、`managerCommands.ts` |

## 依赖审计

运行依赖首次审计发现 React Router 漏洞；完整依赖审计随后还发现 Electron、Vite、打包工具以及 tar、undici 等传递依赖问题。已在现有主版本范围内修复，并更新锁文件和直接开发依赖的最低修复版本：

| 直接依赖 | 修复后安装版本 |
| --- | --- |
| react-router-dom | 7.18.4 |
| electron | 42.11.4 |
| electron-builder | 26.15.3 |
| vite | 8.3.0 |
| esbuild | 0.28.2 |

修复后再次执行完整 `npm audit --json`，报告 **0 个已知漏洞**。此结论针对当前锁文件和审计数据库，不代表对应用本身完成了渗透测试。

## 验证结果

验证环境：Windows、Node.js 22.17.0；以下构建、测试与 IPC 结果已使用修复后的依赖复核。

- 类型检查和生产构建：通过（Vite 8.3.0）。
- 行为及组件测试：8 个测试文件、27 项测试通过。
- 管理器/核心/加固专项：13 组通过，其中 core 29 项、hardening 54 项。
- 完整框架回归：14 组全部通过，包含 665 项历史框架检查；使用更新后的依赖再次验证通过。
- Electron IPC：Electron 42.11.4 上的真实隔离 preload 验证通过。
- AST 工程债门禁：通过。
- 发布完整性、签名和信任审计：**未通过**。当前工作区缺少 release-bundle-manifest、来源证明、签名及信任策略等发布证据；本轮没有伪造证据或执行发布。

## 代码质量评估与剩余事项

| 维度 | 评估 |
| --- | --- |
| 架构与扩展 | 管理器注册表和适配器提供了基础；健康页面已按职责拆分。主进程注册及治理服务仍承担过多职责 |
| 类型约束 | 两端严格 TypeScript 检查有效；全局声明与显式 `any` 仍削弱跨层契约，宜优先拆分 IPC 与领域 DTO |
| 并发与错误处理 | 本轮验证覆盖实际取消、排队、恢复和跨进程错误传递；不能把这些结论扩展为全部第三方 CLI 的集成保证 |
| 测试 | 新增真正的进程和页面行为测试；旧框架仍含较多源码字符串断言，真实 CLI/跨平台完整流程覆盖仍需补齐 |
| 性能 | 健康报告错误可单独重试；页面初次进入仍并行请求约 39 个报告，按工作流按需加载及共享盘点结果仍待优化 |

按 AST 统计 `electron/`、`shared/`、`src/` 中 298 个生产 TypeScript 文件（排除测试）：**6 个文件超过 1,500 行，76 个函数超过 100 行，633 处显式 `any`**。这些是已有债务，不是全部符合新规范的声明。

| 超大文件 | 行数 | 建议按职责拆分 |
| --- | ---: | --- |
| `src/types/global.d.ts` | 5,859 | 领域类型、IPC 请求/结果、全局桥接接口 |
| `electron/services/workspaceGovernance.ts` | 3,770 | 工作区聚合、策略继承、报告导出 |
| `electron/services/readinessGate.ts` | 3,353 | 检查器、策略读取、评分/决策、结果输出 |
| `electron/services/extendedManager.ts` | 3,036 | 生态解析、操作计划、执行、备份存储 |
| `electron/services/supplyChain.ts` | 2,470 | 组件库存、许可证、快照、差异与导出 |
| `electron/main.ts` | 2,136 | 应用生命周期、按领域注册 IPC |

工程债门禁现在检查所有生产 TypeScript 文件的 AST：新增文件不超过 1,500 行、新增函数不超过 100 行，不允许增加显式 `any`；历史例外使用逐文件基线，只能缩减，不应通过重新生成基线接受回退。

后续优先级：先拆分 IPC/领域类型和执行服务，再逐步减少治理报告重复计算，最后补充真实包管理器、完整桌面交互及 Linux/macOS 的集成验收。注册表中的 `planned` 能力仍属于后续需求，不能仅靠页面入口判定已经实现。

## 发布前二次复核（v1.0.3）

再次复核发现并修复：

- 工作区依赖列表在 await 之前检查请求有效性，导致旧项目结果覆盖新项目。提取 `useWorkspaceRequest`，在结果返回后验证请求序号，隔离项目切换、管理器切换、卸载与重复请求；操作计划和手动恢复使用同一机制。
- 命令后的健康刷新仅比较项目路径，无法隔离同一路径中的新操作；现在同时检查操作身份。
- 概览与库存加载错误共享状态，管理器切换还会作废无须失效的概览请求；已分离加载状态与有效期。
- 发布脚本忽略编译失败、失败退出码错误、默认平台使用 Windows 格式、all 实际只构建本机。现在复用明确的平台目标解析器，失败立即终止，不生成成功发布说明。
- 新打包工具不支持 JPG 图标，已无损转换原始图像为 PNG 并更新打包配置。
- 关于窗口写死旧版本，改用 Electron 应用元数据读取版本。
- 导航白名单错误放行任意本地 HTML 和相似开发地址；改为精确的应用入口文件和开发 origin 比较，避免把 IPC 桥接暴露给其他页面。

版本更新为 1.0.3。追加验证：33 项 Vitest 测试与 5 项发布脚本测试通过；生产构建、真实 Electron IPC、依赖审计和工程债门禁通过。Windows x64 安装版和便携版打包通过，解包程序的启动探测通过（仅进程启动检查，不等同于完整 GUI 自动化）。

本次 AST 统计为 299 个生产 TypeScript 文件、6 个超大文件、76 个长函数、630 处显式 any；降低了工作区组件的历史基线。上述历史大文件仍需后续按职责拆分。

本次 GitHub 发布按既有方式提供 Windows 附件和 SHA-256 校验；没有构建 Linux/macOS 附件，没有宣称发布签名或来源证明门禁通过。治理模块的 release-integrity/signature/trust 脚本仍缺少项目报告证据，不是已完成的签名供应链证明。

二次完整框架回归：14 组全部通过，包含 665 项历史检查。
