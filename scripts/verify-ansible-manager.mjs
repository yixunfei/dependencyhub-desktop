import { build } from 'esbuild'
import { mkdtemp, rm, writeFile } from 'fs/promises'
import { tmpdir } from 'os'; import { join } from 'path'; import { pathToFileURL } from 'url'
const workDir=await mkdtemp(join(tmpdir(),'dependencyhub-ansible-verifier-')); const outputFile=join(workDir,'ansible-verifier.mjs')
const runner=String.raw`
import {mkdtemp,rm,writeFile} from 'fs/promises'; import {tmpdir} from 'os'; import {join} from 'path'; import {ManagerWorkspaceService} from './electron/services/managerWorkspace'
const roots=[];const checks=[];const assert=(v,m)=>{if(!v)throw new Error(m);checks.push(m)};const fixture=async()=>{const c=await mkdtemp(join(tmpdir(),'dependencyhub-ansible-'));roots.push(c);return c}
const main=async()=>{const cwd=await fixture();await writeFile(join(cwd,'requirements.yml'),['collections:','  - name: community.general','    version: 8.6.0','  - name: https://example.test/collection.tar.gz','    type: url','roles:','  - name: geerlingguy.nginx','    version: 3.1.0'].join(String.fromCharCode(10)));const service=new ManagerWorkspaceService();const d=service.descriptors().find(x=>x.managerId==='ansible');assert(d?.status==='preview','Ansible uses preview adapter');assert(d?.capabilities.health===true,'Ansible exposes health capability');const inv=await service.inventory(cwd,'ansible');assert(inv.some(x=>x.name==='community.general'&&x.type==='collection'),'Ansible parses requirements');assert(inv.some(x=>x.name==='geerlingguy.nginx'),'Ansible parses role requirements');for(const [op,cmd] of [['sync','ansible-galaxy collection install -r requirements.yml'],['install','ansible-galaxy collection install -r requirements.yml'],['update','ansible-galaxy collection install -r requirements.yml --upgrade'],['list','ansible-galaxy collection list'],['audit','ansible-galaxy collection list']]){const p=await service.plan(cwd,'ansible',{operation:op});assert(p.command===cmd,'Ansible '+op+' command')}const h=await service.health(cwd,'ansible');assert(h.findings.some(x=>x.id==='ansible-no-lockfile'),'Ansible reports no standard lockfile');const missing=await fixture();const mh=await service.health(missing,'ansible');assert(mh.findings.some(x=>x.id==='ansible-manifest-missing'),'Ansible reports missing requirements');console.log('Ansible manager verification passed ('+checks.length+' checks)')};try{await main()}finally{await Promise.all(roots.map(r=>rm(r,{recursive:true,force:true})))}
`
try{await build({stdin:{contents:runner,resolveDir:process.cwd(),sourcefile:'ansible-verifier.ts',loader:'ts'},outfile:outputFile,bundle:true,platform:'node',format:'esm',target:'node20',logLevel:'silent',banner:{js:"import { createRequire } from 'module'; const require = createRequire(import.meta.url);"},plugins:[{name:'ansible-stubs',setup(api){api.onResolve({filter:/^\.\/(toolchain|commandRunner)$/},args=>args.importer.endsWith('extendedManager.ts')?{path:args.path,namespace:'ansible-stub'}:undefined);api.onLoad({filter:/.*/,namespace:'ansible-stub'},args=>args.path==='./toolchain'?{loader:'ts',contents:"export type ToolName=string; export async function resolveToolBin(tool:string){return tool;}"}:{loader:'js',contents:"export async function runLoggedCommand(){return {stdout:'',stderr:''}}"})}}]});await import(pathToFileURL(outputFile).href)}finally{await rm(workDir,{recursive:true,force:true})}
